import { Typography } from "@mui/material";

const Error = () => {
  return (
    <Typography variant="body2" color="red">
      Error updating user role, please contact support.
    </Typography>
  );
};

export default Error;
